phase:	SETUP (ABSTRACT)

text:
-->	setup: q = .clump(1);
	if (x) {
		*(q) = 1;
	}

rconst:
	$0: ptr:{?}	"q"
	$1: int:{?}	"x"

stack:
	---------------------------- setup
	0: |1| {0:<rconst:$0>} (int * q, π)
	1: |1| {0:<rconst:$1>} (int x, π)
	---------------------------- modify0



(0db)
