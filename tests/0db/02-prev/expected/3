phase:	ABSTRACT (SETUP)

text:
-->	setupv .clump(1);
	call .clump(1);
	movret #R0;
	q = #R0;

rconst:
	$0	"q"
	$1	"x"

	|- -$0 <= 32767
	|- $0 <= 32767
	|- -$1 <= 32767
	|- $1 <= 32767

stack:
	---------------------------- inter
	---------------------------- setup
	---------------------------- findsetup
	0: |1| {0:<rconst:$0>} (int * q, π)
	1: |1| {0:<rconst:$1>} (int x, π)
	---------------------------- modify0



(0db)
